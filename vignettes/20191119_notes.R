11/19/2019
turning functions into R package. New text file - descripiton file with package info.


install.packages("roxygen2")

building a R package: seperate out 'how to' into a serperate directory > new folder = vignettes
Add new > markdownfile "package instructions"
Copy all notes and code (not the functions though) into the markdown file. Delete all notes in the origional function_.R files (only have code and functions in those files)
Save in vignettes

build > configure build tools> /cloud/project